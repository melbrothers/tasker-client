import { ConvertDateTimeToFromNowPipe } from './convert-date-time-to-from-now.pipe';

describe('ConvertDateTimeToFromNowPipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertDateTimeToFromNowPipe();
    expect(pipe).toBeTruthy();
  });
});
